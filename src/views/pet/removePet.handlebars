<h1>{{title}}</h1>
<div id="error" class="error">{{error}}</div>
{{#if loggedIn}}
    <form method="post" id="removePet">
        <button type="submit" id="rbtn">Remove this pet</button>
    </form>
{{else}}
    <p>You must be logged in to remove a pet!</p>
{{/if}}